# Render Static Site — multi-page (home, /aquatrack/, /links/)
# Do NOT rewrite all routes to index.html (that breaks subpages).
services:
  - type: web
    name: oeosolution-home
    runtime: static
    buildCommand: "echo static"
    staticPublishPath: .
